# Anhloom Backend (Phase 4)

FastAPI services for AI features will be implemented in Phase 4.

This directory contains a stub `Dockerfile` so `docker-compose` can reference the backend service in later phases.
